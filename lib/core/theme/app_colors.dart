import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary — warm sunrise orange
  static const Color primary = Color(0xFFFF7043);
  static const Color primaryLight = Color(0xFFFF8A65);
  static const Color primaryDark = Color(0xFFE64A19);

  // Secondary — calm sage green (vitality, nature)
  static const Color secondary = Color(0xFF66BB6A);
  static const Color secondaryLight = Color(0xFF81C784);
  static const Color secondaryDark = Color(0xFF388E3C);

  // Accent — soft golden yellow (energy, warmth)
  static const Color accent = Color(0xFFFFCA28);
  static const Color accentLight = Color(0xFFFFD54F);
  static const Color accentDark = Color(0xFFFFB300);

  // Neutrals
  static const Color background = Color(0xFFFFFBF7); // warm off-white
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceWarm = Color(0xFFFFF3E0); // very light peach

  // Text
  static const Color textPrimary = Color(0xFF2D2D2D);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Utility
  static const Color divider = Color(0xFFEEEEEE);
  static const Color success = Color(0xFF66BB6A);
  static const Color error = Color(0xFFEF5350);
}
